<odoo>
    <record id="view_move_form_inherit_commission" model="ir.ui.view">
        <field name="name">account.move.form.inherit.commission</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_view_commission_ledgers"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-line-chart"
                    invisible="move_type not in ('out_invoice', 'out_refund')"
                >
                    <field name="commission_ledger_count" string="Commission" widget="statinfo"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
